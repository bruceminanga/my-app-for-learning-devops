apiVersion: v1
kind: ConfigMap
metadata:
  name: my-app-config
  namespace: default
data:
  LOG_LEVEL: "debug"        # ← lowercase
  DATABASE_HOST: "db.production.internal"
  settings.properties: |
    theme=dark
    max_retries=5
    timeout_seconds=30