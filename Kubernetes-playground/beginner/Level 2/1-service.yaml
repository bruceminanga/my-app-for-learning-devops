apiVersion: v1
kind: Service
metadata:
  name: real-web-service
spec:
  type: NodePort
  selector:
    app: web # This connects directly to my deployment!
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080